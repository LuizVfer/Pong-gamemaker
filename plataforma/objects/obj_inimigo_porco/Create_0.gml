// Inherit the parent event
event_inherited();
//personalizando as variaveis
// ele vai decidir se vai andar para direita ou esquerda
vel = choose (1, -1);
estado = "normal"

pega_bomba = true 

andando = choose(true, false);
if (andando)
{
	if(andando)
	{
		velh= choose(vel, -vel)
	}
	else
	{
		velh = 0;
	}
}
tempo_decidir_andar = room_speed * 3;
